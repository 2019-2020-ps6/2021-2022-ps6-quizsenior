
export interface QuizGame {
  id: string;
  correctAnswers: string;
  incorrectAnswers: string;
  quiz: string;
  nbRepetition: string;
  answers: string;
  // user?: string;
  // answersDmla: Map<string, string>;
}
